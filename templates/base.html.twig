<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Crochet D'Or{% endblock %}</title>

    {# ✅ Bootstrap CSS via CDN #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Optional: Font Awesome #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Adam+Script&display=swap" rel="stylesheet">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% endblock %}
</head>

<body>
{# Bootstrap Navbar #}
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #8bd1d2;">
    <div class="container-fluid">
        {# Logo & Brand (LEFT) #}
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_welcome') }}">
            <img src="{{ asset('image/logo.png') }}" alt="Logo" width="30" height="30" class="me-2">
            <strong>Crochet D'Or</strong>
        </a>

        {# Toggler for mobile #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        {# Navigation content #}
        <div class="collapse navbar-collapse justify-content-between" id="navbarContent">
            {# CENTER: Navigation Links #}
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_welcome') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('shop') }}">Shop</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_products') }}">Admin</a>
                </li>
            </ul>

            {# RIGHT: Icons #}
            <ul class="navbar-nav">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-magnifying-glass" style="color: #fff;"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-heart" style="color: #fff;"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-user" style="color: #fff;"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-shopping-cart" style="color: #fff;"></i>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<main class="container mt-4">
    {% block body %}<div class="row">

        </div>{% endblock %}
</main>

{# Bootstrap JS via CDN (for toggler menu) #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-XVdNgI+Mcb4NzX2xymStsOvD8d7uw50/hFd8l5X9/fjFz6m6B3/8Gpnq67UeSz8P"
        crossorigin="anonymous"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
